import ExpoModulesCore

public class LnExtensionExecutorBridgeView: ExpoView {
    public required init(appContext: AppContext? = nil) {
        super.init(appContext: appContext)
        // This module doesn't need a view implementation
        // but Expo requires this file to exist
    }
}
